# 海龟汤 AI · 使用说明（傻瓜版）

这份说明手把手带你从零开始运行这个游戏。整个过程只需要三步：装 Java → 打一个包 → 双击启动。

---

## 第一步：装 Java 17（只需一次）

游戏需要 JDK 17 才能运行。你电脑上如果已经装了，可以跳过这步。

1. 打开下载页：https://adoptium.net/temurin/releases/?version=17
2. 选择你的系统（Windows），下载 `.msi` 安装包
3. 双击安装，一路「下一步」即可（**勾选**安装向导里设置 PATH/JAVA_HOME 的选项，如果没有就保持默认）
4. 验证是否装好：按 `Win + R`，输入 `cmd` 回车，在黑色窗口里输入：

```
java -version
```

如果显示类似 `openjdk version "17.0.x"` 就成功了。如果显示 11 或更低版本，说明电脑上有旧的 Java 抢先了，继续看下面「常见问题」。

---

## 第二步：打包（只需一次）

打包会把代码变成一个 `.jar` 文件，双击脚本就用它来运行。

1. 确认你的代码里已经有 `target\turtle-soup-ai-*.jar` 这个文件（如果之前跑过 Maven 打包，通常已经有了）
2. 如果没有，在代码目录（能看到 `pom.xml` 的文件夹）打开命令行，执行：

```
mvnw.cmd clean package
```

> 你的项目里已经带了 `mvnw.cmd`（Maven Wrapper），会自动下载 Maven，不用另外装。

打包成功后，`target` 文件夹里会出现 `turtle-soup-ai-0.0.1-SNAPSHOT.jar`。

---

## 第三步：双击启动

1. 双击代码目录里的 **`start.bat`**
2. 黑色窗口会显示「正在启动，浏览器访问 http://localhost:8080」
3. 打开浏览器，输入 http://localhost:8080 ，开始玩！

> 如果弹窗提示你还没有设置 API Key，`start.bat` 会问你「请输入 DashScope API Key」，把你在阿里云百炼（DashScope）申请的 key 粘贴进去回车即可。这个 key 只在本次运行有效，不会写进任何文件。

停止游戏：直接关掉那个黑色窗口。

---

## 常见问题

**Q：双击 start.bat 一闪而过 / 报错说找不到 Java？**
A：说明电脑上没有 JDK 17，或者装了但系统里指向了旧版本。两个办法：
- 重新装 JDK 17 并在安装时勾选「设置 JAVA_HOME」；
- 或者自己指定 Java 位置：右键编辑 `start.bat`，找到 `JAVA_EXE` 那一块，把路径改成你 JDK17 的实际位置，例如 `set "JAVA_EXE=C:\Program Files\Java\jdk-17\bin\java.exe"`。

**Q：提问后一直没反应，或者提示「AI 服务暂时不可用」？**
A：一般是 API Key 没生效。确认你在阿里云百炼控制台已经开通「通义千问」模型权限，并且 key 填对了。也可以在系统环境变量里永久设置 `DASHSCOPE_API_KEY`（设置为你的 key），这样以后启动就不用每次输了。

**Q：怎么修改端口？**
A：编辑 `src\main\resources\application.yaml`，把 `server.port: 8080` 改成你想要的端口，重新打包即可。

**Q：日志在哪看？**
A：游戏运行日志写在 `logs\game-log.txt`（按天滚动）。排查问题时优先看这里。

---

## 一句话总结

**装 Java 17 → 打包（mvnw package）→ 双击 start.bat → 浏览器打开 localhost:8080。**
